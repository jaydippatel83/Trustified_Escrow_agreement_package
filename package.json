{
  "name": "package_agreement",
  "version": "0.1.0",
  "private": true,
  "engineStrict": false,
  "dependencies": {
    "@testing-library/jest-dom": "^5.16.4",
    "@testing-library/react": "^13.1.1",
    "@testing-library/user-event": "^13.5.0",
    "crypto": "^1.0.1",
    "react": "^18.1.0",
    "react-dom": "^18.1.0",
    "react-scripts": "4.0.3",
    "stream": "^0.0.2",
    "trustified_escrow_agreement_package": "^0.1.0",
    "web-vitals": "^2.1.4"
  },
  "babel": {
    "presets": [
      "@babel/preset-react",
      "@babel/env"
    ]
  },
  "scripts": {
    "start": "if-node-version '>= 17' && react-scripts --openssl-legacy-provider start || react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
